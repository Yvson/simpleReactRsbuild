# simpleReactRsbuild
React app created via Rsbuild tool.

## Installation

From root folder, run:

```sh
npm install
npm run dev
```

To check the application running, go to:

**React App:**
> localhost:3000
